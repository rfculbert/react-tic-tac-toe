import React from 'react';

const Square = (props) => {
    return (
        
        <div className="props.propVariable">
            square
        </div>
    );
};

export default Square;

const handleClick = () => {
    
}